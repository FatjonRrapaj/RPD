import {StyleSheet} from 'react-native';

import {DARK_TEXT} from '../../constants/colors';

export default StyleSheet.create({
  text: {
    fontFamily: 'Avenir',
    color: DARK_TEXT,
    fontSize: 14,
  },
});
